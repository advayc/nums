---
title: 'Badge (SVG)'
openapi: 'GET /badge'
---

Render an SVG badge with the current count.

Query params: `id`, `label`, `style`, `bg`, `labelColor`, `valueColor`, `font`.

Example
```bash
open "https://<deployment>/badge?id=home&style=terminal&label=hits"
```
