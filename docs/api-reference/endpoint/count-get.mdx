---
title: 'Read count (JSON)'
openapi: 'GET /count'
---

Return the current hit count for an id. Supports `format=txt|text` for plain text.

Example
```bash
curl "https://<deployment>/count?id=home"
```
